/**
 * @cre/shared
 *
 * Shared types, schemas, and utilities used across the monorepo
 * (apps/web, packages/api, packages/db, ...).
 *
 * This module currently has no exports; add them here as shared code
 * is introduced.
 */
export {};